<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LoginPage.Views.RegistrationPage"
             xmlns:viewModels="clr-namespace:LoginPage.ViewModels">

    <ContentPage.BindingContext>
        <viewModels:MainViewModel>

        </viewModels:MainViewModel>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <Image Source="iconintec.png" x:Name="imgLogo" HeightRequest="150" WidthRequest="150"/>
            <Entry Text="{Binding UserName}" Placeholder="Name"/>
            <Entry Text="{Binding Email}" Placeholder="Email"/>
            <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True"/>
            <Entry Text="{Binding ConfirmPassword}" Placeholder="ConfirmPassword" IsPassword="True"/>
            <Button Command="{Binding OnRegistrationCommand}" Text="Register" TextColor="White" BackgroundColor="Red"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>